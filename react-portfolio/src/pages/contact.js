import { Typography } from '@mui/material';
import * as React from 'react';

export default function Contact() {
    return(
        <React.Fragment>
            <Typography>
                Contact me
            </Typography>
        </React.Fragment>
    );
}