import { Typography } from '@mui/material';
import * as React from 'react';

export default function Resume() {
    return(
        <React.Fragment>
            <Typography>
                My Resume
            </Typography>
        </React.Fragment>
    );
}